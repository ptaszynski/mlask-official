Metadata-Version: 2.4
Name: mlask-official
Version: 0.5.0
Summary: ML-Ask Official — high-performance Japanese emotion analysis (original system, Python rewrite)
Author-email: Michal Ptaszynski <ptaszynski@ieee.org>
Maintainer-email: Michal Ptaszynski <ptaszynski@ieee.org>
License: BSD-2-Clause
Project-URL: Homepage, https://github.com/michalptaszynski/mlask-official
Project-URL: Bug Tracker, https://github.com/michalptaszynski/mlask-official/issues
Project-URL: Changelog, https://github.com/michalptaszynski/mlask-official/blob/main/CHANGELOG.md
Project-URL: Citation, https://github.com/michalptaszynski/mlask-official/blob/main/CITATION.cff
Keywords: nlp,japanese,emotion,sentiment,affect,ml-ask,affective-computing
Classifier: Development Status :: 4 - Beta
Classifier: Intended Audience :: Science/Research
Classifier: License :: OSI Approved :: BSD Software License
Classifier: Programming Language :: Python :: 3
Classifier: Programming Language :: Python :: 3.9
Classifier: Programming Language :: Python :: 3.10
Classifier: Programming Language :: Python :: 3.11
Classifier: Programming Language :: Python :: 3.12
Classifier: Programming Language :: Python :: 3.13
Classifier: Topic :: Text Processing :: Linguistic
Classifier: Topic :: Scientific/Engineering :: Artificial Intelligence
Classifier: Natural Language :: Japanese
Classifier: Operating System :: OS Independent
Requires-Python: >=3.9
Description-Content-Type: text/markdown
Requires-Dist: mecab-python3>=1.0
Requires-Dist: pyahocorasick>=2.0
Requires-Dist: typer>=0.9
Provides-Extra: app
Requires-Dist: streamlit>=1.30; extra == "app"
Requires-Dist: plotly>=5.0; extra == "app"
Requires-Dist: pandas>=2.0; extra == "app"
Provides-Extra: fugashi
Requires-Dist: fugashi[unidic-lite]>=1.3; extra == "fugashi"
Provides-Extra: deps
Requires-Dist: ginza>=5.1; extra == "deps"
Requires-Dist: ja-ginza>=5.1; extra == "deps"
Provides-Extra: dev
Requires-Dist: pytest>=7.0; extra == "dev"
Requires-Dist: pytest-benchmark; extra == "dev"
Provides-Extra: all
Requires-Dist: streamlit>=1.30; extra == "all"
Requires-Dist: plotly>=5.0; extra == "all"
Requires-Dist: pandas>=2.0; extra == "all"
Requires-Dist: fugashi[unidic-lite]>=1.3; extra == "all"
Requires-Dist: ginza>=5.1; extra == "all"
Requires-Dist: ja-ginza>=5.1; extra == "all"

# ML-Ask Official v0.5

**eMotive eLement and Expression Analysis system — official Python rewrite**

High-performance Japanese emotion analysis. Original ML-Ask system by Michal Ptaszynski, Pawel Dybala, Rafal Rzepka and Kenji Araki.

---

## About ML-Ask

ML-Ask (*eMotive eLement and Expression Analysis system*) is a keyword-based rule
system for automatic affect annotation of Japanese utterances.

---

## Features

- **Combined and expanded dictionaries** — Nakamura's original *Dictionary of Emotive
  Expressions* merged with the Wang & Isomura (2024) two-dictionary expansion
  (Hiejima's and Murakami's emotion dictionaries, plus automatically extracted
  expressions). Total: ~4,700 entries across 10 emotion classes.
- **Russell's 2D circumplex model** of affect — every emotion class is located on
  a (valence, arousal) plane, and aggregate sentence orientation is reported as
  `valence` (POSITIVE / NEGATIVE / NEUTRAL, optionally `mostly_*`) and `activation`
  (ACTIVE / PASSIVE / NEUTRAL).
- **Plutchik-wheel colour palette** — all 10 emotion classes are colour-coded by the
  hue angles of Plutchik's published wheel for a familiar, paper-ready palette.
- **Dual Aho-Corasick matching** — two automata are built at startup, one over fully
  lemmatised dictionary entries (covers verb inflections) and one over particle-stripped
  content forms (covers particle-omission variants such as 腹がたつ ↔ 腹たつ).
  Both automata scan in a single O(n + k) pass per sentence.
- **CVS (Contextual Valence Shifters)** — 108 Japanese negation patterns reverse
  emotion polarity when applied (`嫌いではない` → positive, not negative).
- **Three-state emotive distinction** — `analyze()` returns `emotive: bool` even when
  no specific emotion word is detected, so callers can distinguish emotive-but-
  unclassifiable sentences (e.g., interjections only) from fully non-emotive ones.

---

## What's new in v0.5

- **On-disk lemma cache** — second cold start is sub-millisecond
  (~25 ms saved); MD5-invalidated, lives in `$XDG_CACHE_HOME/mlask_official/`.
- **Optional UniDic backend** via fugashi:
  `MLAskOfficial(backend="fugashi")` (install `mlask-official[fugashi]`).
- **Optional GiNZA dependency-tree CVS** for long-distance negation:
  `MLAskOfficial(use_dependency_cvs=True)` (install `mlask-official[deps]`).
- **Fuzzy emoteme matching** — stretched-vowel input like `やばーーーい` is
  normalised to `やばーい` before scanning.
- **Modern dictionaries** — emoji (`😭` → aware, `🔥` → takaburi…), kaomoji
  (`(T_T)`, `(>_<)`), gyaru-go / wasei-go (`ガチ`, `推し`, `草`), and katakana
  borrowings (`ムカつく`, `ショック`, `テンション`) added per emotion class.
- **`mlask` command-line interface** — `analyze`, `batch`, `benchmark`,
  `extract` subcommands; stdin/stdout piping; JSON / CSV / pipe output formats.
- **Streaming + multiprocessing APIs** — `analyze_stream(iterable)` for
  memory-light corpus processing; `analyze_parallel()` and auto-on at
  ≥ 50,000 sentences via the standard `analyze_batch()`.
- **JA/EN language toggle** in the Streamlit app — UI strings and emotion
  labels switch between English (`Yorokobi (joy)`) and Japanese (`喜び`).
- **Time-series view** — timestamp-prefixed input (ISO date per line) renders
  an emotion-prevalence-over-time chart in the batch tab.
- **Mobile-responsive layout** — collapses side-by-side columns vertically
  at narrow viewports.
- **Packaging** — `CITATION.cff` and `conda-recipe/meta.yaml` shipped.

---

## What's new in v0.4

- **API rename** `orientation` → `valence` to match Russell's canonical terminology.
  Breaking change; no alias.
- **Batch tab fixed** — replaced `st.metric` (which fails with
  `TypeError: Failed to fetch dynamically imported module …Metric.*.js`) with
  static HTML KPI cards. The failure mode is now structurally impossible.
- **Publication-quality visualisations** — Russell circumplex is square-aspect with
  serif typography, sqrt-scaled markers, axis-unit labels; every chart now exposes a
  PNG download button at 2× scale; batches >20 sentences render as an annotated
  emotion × sentence heatmap instead of a hidden bar chart.
- **CSV `emotive` column** simplified from three states (`yes`/`emotive_noclass`/`no`)
  to binary `yes`/`no`.
- App emoji and favicon: ❤️.
- Concise sidebar; quick-example buttons moved below the input (full-width row).

See `CHANGELOG.md` for the full history.

---

## Emotion classes

| Name       | English gloss | Valence | Arousal | Plutchik hue |
|------------|--------------|---------|---------|-------------|
| yorokobi   | joy          | POS     | ACT     | yellow      |
| suki       | affection    | POS     | —       | yellow-green|
| yasu       | relief       | POS     | PAS     | green-yellow|
| takaburi   | excitement   | NorP    | ACT     | orange      |
| odoroki    | surprise     | NorP    | ACT     | teal-cyan   |
| haji       | shame        | NorP    | —       | rose-purple |
| aware      | sadness      | NEG     | PAS     | royal blue  |
| iya        | disgust      | NEG     | —       | dark orchid |
| kowa       | fear         | NEG     | ACT     | green       |
| ikari      | anger        | NEG     | ACT     | crimson     |

### Representative emotion

For sentences where ML-Ask detects multiple emotion classes, the **representative
emotion** is the single class chosen as the dominant one for the sentence. The
heuristic — inherited from the original Perl ML-Ask — is:

> *The class whose longest matched expression has the most characters.*

For example, in *「腹がたって仕方ない、もう嫌だ」* both *ikari* (`腹が立つ`) and *iya*
(`嫌だ`) match; *ikari* wins because `腹が立つ` is longer than `嫌だ`. The intuition
is that longer dictionary entries are more specific — and therefore more diagnostic
of the speaker's emotion — than shorter, more generic ones.

Returned as `result["representative"] = (class_name, [matching_words])`.

---

## Installation

ML-Ask Official runs on **Linux, macOS and Windows (WSL recommended)** with
**Python 3.9 – 3.13**.  It depends on the [MeCab](https://taku910.github.io/mecab/)
morphological analyser, which is a *system* package (not Python), so the install
is split into two parts:

1. install MeCab + a Japanese dictionary at the OS level,
2. install the `mlask-official` Python package inside a virtualenv.

### Step 1 — Install MeCab + a Japanese dictionary

#### macOS (Homebrew)

```bash
brew install mecab mecab-ipadic
```

Verify:

```bash
echo "今日は嬉しい" | mecab
```

You should see one token per line and an `EOS` marker.

#### Ubuntu / Debian

```bash
sudo apt-get update
sudo apt-get install -y mecab libmecab-dev mecab-ipadic-utf8
```

#### Fedora / RHEL

```bash
sudo dnf install mecab mecab-devel mecab-ipadic
```

#### Arch Linux

```bash
sudo pacman -S mecab mecab-ipadic
```

#### Windows

Native Windows MeCab is fragile — **the recommended path is Windows Subsystem
for Linux (WSL2)**: install Ubuntu under WSL and follow the Ubuntu instructions
above.  If you must run on bare Windows, see the
[mecab-python3 README](https://github.com/SamuraiT/mecab-python3#windows) for
the MSVC build steps.

### Step 2 — Create a Python virtual environment

Strongly recommended (keeps mlask's dependencies out of your system Python):

```bash
python3 -m venv .venv
source .venv/bin/activate          # macOS / Linux
# .venv\Scripts\activate.bat       # Windows cmd
# .venv\Scripts\Activate.ps1       # Windows PowerShell
```

Make sure `python --version` reports 3.9 or newer.

### Step 3 — Install `mlask-official`

#### From PyPI (once published)

```bash
pip install mlask-official            # core: analyzer + CLI
pip install 'mlask-official[app]'     # + Streamlit web app
pip install 'mlask-official[fugashi]' # + UniDic backend via fugashi
pip install 'mlask-official[deps]'    # + GiNZA dependency-tree CVS
pip install 'mlask-official[all]'     # everything above
```

#### From a source checkout (this folder)

```bash
cd mlask-official_0.5
pip install -e .                      # editable core install
pip install -e '.[all]'               # editable + every extra
```

The base install pulls in `mecab-python3`, `pyahocorasick`, and `typer`
automatically.

### Step 4 — Verify the installation

#### Python API

```bash
python -c "from mlask_official import MLAskOfficial; \
print(MLAskOfficial().analyze('今日は嬉しい！')['valence'])"
```

You should see:

```
POSITIVE
```

#### CLI

```bash
mlask --help
echo "彼のことは嫌いではない！" | mlask analyze --format pipe
```

The pipe-format output should look like:

```
彼のことは嫌いではない！|emotions:(2)|YOR:嫌い*CVS 嫌いな*CVS|SUK:嫌い*CVS 嫌いな*CVS||2D|POSITIVE|NEUTRAL
```

#### Streamlit app

```bash
# From the source checkout
bash run_app.sh                       # → http://localhost:8501
bash run_app.sh --server.port 8505    # custom port

# If you installed from PyPI
streamlit run -m mlask_official.streamlit_app
```

Open the URL in your browser and try the *Quick examples* under the input box.

### Step 5 — Troubleshooting

**`RuntimeError: Failed initializing MeCab` (`no such file: /usr/local/etc/mecabrc`)**

`mecab-python3` can't find `mecabrc`.  Find it and pass it explicitly:

```bash
mecab-config --sysconfdir   # → e.g. /opt/homebrew/etc

python -c "from mlask_official import MLAskOfficial; \
print(MLAskOfficial(mecab_arg='-r /opt/homebrew/etc/mecabrc').analyze('嬉しい'))"
```

Or pass `--mecab-arg "-r /opt/homebrew/etc/mecabrc"` to the CLI.  The Streamlit
app has a *MeCab arguments* field in the sidebar for the same purpose.

**`No module named 'MeCab'`**

`mecab-python3` failed to compile against your system MeCab.  Re-install with
verbose output:

```bash
pip install --force-reinstall --verbose mecab-python3
```

On macOS the most common cause is missing Xcode command-line tools
(`xcode-select --install`).

**`No module named 'fugashi'` / `'spacy'`**

Optional extras aren't installed.  Either disable the feature
(`MLAskOfficial(backend="mecab", use_dependency_cvs=False)`) or install the
relevant extra group from step 3.

**Stale lemma cache after a manual dictionary edit**

The cache is invalidated by file *content* (MD5), so saving the file will
already invalidate it.  To force a rebuild explicitly:

```bash
rm -rf ~/.cache/mlask_official
# or per-call:
python -c "from mlask_official import MLAskOfficial; MLAskOfficial(use_cache=False)"
```

---

## Usage

```python
from mlask_official import MLAskOfficial

a = MLAskOfficial()

# Inflected form — handled by full-lemma automaton
r = a.analyze("身の毛がよだった！")
print(r["emotion"])       # {'kowa': ['身の毛がよだつ']}
print(r["valence"])       # 'NEGATIVE'
print(r["activation"])    # 'ACTIVE'
print(r["emotive"])       # True

# Particle dropped — handled by content-lemma automaton
r = a.analyze("腹たつ！")
print(r["emotion"])       # {'ikari': ['腹が立つ≈']}

# Negation via CVS
r = a.analyze("彼のことは嫌いではない！")
print(r["valence"])       # 'POSITIVE'  ← 嫌い → CVS flip → yorokobi/suki

# Emotive but no classifiable emotion
r = a.analyze("あーもう！！")
print(r["emotion"])       # None
print(r["emotive"])       # True  ← emotemes detected
print(r["intensifier"])   # {'emotemes': ['！','！'], 'interjections': ['あー','もう']}

# Non-emotive
r = a.analyze("今日は晴れです。")
print(r["emotion"])       # None
print(r["emotive"])       # False
```

---

## Command-line interface (v0.5)

```bash
# Single sentence (any of stdin, --text)
echo "腹が立つ"               | mlask analyze --format pipe
echo "彼のことは嫌いではない！" | mlask analyze --format json

# Batch a file
mlask batch -i corpus.txt -o results.csv --format csv
mlask batch -i corpus.txt -o results.json --format json --parallel
mlask batch -i corpus.txt --format pipe > results.txt

# Throughput benchmark
mlask benchmark --sentences 10000
mlask benchmark --sentences 100000 --parallel -j 8

# Mine candidate emotive expressions from a corpus (manual-review TSV)
mlask extract corpus.txt --output candidates.tsv --min-freq 5
```

All commands accept `--backend mecab|fugashi` and `--mecab-arg "-r /path/to/mecabrc"`.

## Run the Streamlit app

```bash
bash run_app.sh                       # → http://localhost:8501
bash run_app.sh --server.port 8505
```

Every chart in the app shows a small camera icon in its top-right corner — click it
to download a 2× resolution PNG suitable for a paper figure.

---

## Citation

When using ML-Ask in research, please cite both of the following:

> Ptaszynski, M., Dybala, P., Rzepka, R., Araki, K., & Masui, F. (2017).
> *ML-Ask: Open source affect analysis software for textual input in Japanese.*
> Journal of Open Research Software, 5(1), 16-16.

```bibtex
@article{ptaszynski2017ml,
  title={ML-Ask: Open source affect analysis software for textual input in Japanese},
  author={Ptaszynski, Michal and Dybala, Pawel and Rzepka, Rafal and Araki, Kenji and Masui, Fumito},
  journal={Journal of Open Research Software},
  volume={5},
  number={1},
  pages={16--16},
  year={2017}
}
```

> Wang, L., Isomura, S., Ptaszynski, M., Dybala, P., Urabe, Y., Rzepka, R., &
> Masui, F. (2024). *The limits of words: expanding a word-based emotion analysis
> system with multiple emotion dictionaries and the automatic extraction of
> emotive expressions.* Applied Sciences, 14(11), 4439.

```bibtex
@article{wang2024limits,
  title={The limits of words: expanding a word-based emotion analysis system with multiple emotion dictionaries and the automatic extraction of emotive expressions},
  author={Wang, Lu and Isomura, Sho and Ptaszynski, Michal and Dybala, Pawel and Urabe, Yuki and Rzepka, Rafal and Masui, Fumito},
  journal={Applied Sciences},
  volume={14},
  number={11},
  pages={4439},
  year={2024},
  publisher={MDPI}
}
```

---

## License

BSD 2-Clause.
